/**
 * "Science, signed" — the category copy system.
 *
 * Palonur's buyers flip to purchasing at a moment of epistemic collapse:
 * a remedy that failed + free advice that contradicts itself (Google,
 * ChatGPT, forums) + a real stake (tomorrow's meeting, a parent's health,
 * a spouse who's worried). What they are buying is permission to stop
 * searching. Every buying surface leads with that struggling moment and
 * the promise of closure — never with tiers, features, or plan lists.
 *
 * The category name gives buyers vocabulary to ask for it (the way
 * HubSpot named "inbound"): **Science, signed.**
 *
 * ── Checklist for every buying surface ─────────────────────────────────
 * Each rewritten surface must hit ALL THREE job dimensions at once:
 *  · Functional — a situation-specific answer grounded in a named
 *    scientist's actual published work, including an honest refusal
 *    when the research doesn't cover the question.
 *  · Emotional — relief: confident, calm, done searching.
 *  · Social — a citation you can show a spouse or a doctor;
 *    "I go to the source."
 * Never lead with one dimension alone. Never say "AI-powered" (or any
 * equivalent); the honesty features — citations, a named steward, the
 * UNCOVERED refusal — are the differentiator.
 *
 * ── Struggling-moment hook per pillar ──────────────────────────────────
 *  · Sleep: the 3 a.m. wakeup after a fix that didn't hold.
 *  · Steward pillars: the contradiction crisis after a life change —
 *    every free answer disagrees with the last one.
 */

/** The category name. Use verbatim, capitalized exactly like this. */
export const SCIENCE_SIGNED = "Science, signed";

/** The closure promise — the moment the customer is actually buying. */
export const STOP_SEARCHING = "Stop searching. Ask the scientist.";

/** One-liner definition, reusable wherever the category needs unpacking. */
export const CATEGORY_ONE_LINER =
  "One answer from a named scientist's own published research — cited to " +
  "the study so you can check it (or show your doctor), and honest enough " +
  "to say so when the research doesn't cover you yet.";
